# Teaching Notes — Designing Distributed Systems

## Preferences
- **Dark theme always.** Light backgrounds hurt his eyes. `assets/course.css` is dark by default; keep new lessons/reference docs on the shared vars (never hardcode light `#fff`/`#333`). Applies to all future learning sessions, not just this track.
- Mission: **system design interviews**. Frame every pattern as: *what it is → when to reach for it → the tradeoff you'd defend in an interview.*
- **Concepts-first.** Minimal setup. No real clusters unless a pattern genuinely needs one.
- Go **chapter by chapter in book order**, but always land the interview angle.
- Voice: casual but professional, short sentences, no em dashes, bullets over paragraphs (matches Tarun's house style).

## Cadence
- ~1 lesson per chapter; split a chapter into 2 lessons if it's heavy (Ch 2, Ch 6 likely).
- Every lesson: 1 tangible interview-usable win + a retrieval quiz + primary-source pointer.

## Calibration (update as evidence arrives)
- Working software dev, builds front-end-heavy web tools. Comfortable: HTTP, APIs, general web.
- Unconfirmed depth on: Kubernetes, containers, distributed-systems patterns. **Assume light** until a quiz proves otherwise — adjust ZPD from there.

## Open threads
- Find a bank of practice interview prompts mapped to each pattern (see RESOURCES Gaps).
