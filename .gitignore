.env
.env.local
secrets/
*-service-account*.json
# Turnstile/Resend secrets dropped by scripts/cloudflare-go-live.mjs.
# Load them with `wrangler secret put NAME < file`, then delete the file.
go-live.*.secrets.env
.DS_Store
*.log
__pycache__/
node_modules/
.claude/settings.local.json
audits/
daily/dashboard/today.json
# Playwright MCP session artifacts — accessibility-tree snapshots and console
# logs, written on every browser verification. Regenerated each run, tied to one
# session, and useless to anyone else. They were never meant to be committed.
.playwright-mcp/
