# Kesri Enterprise — Reports

Dated monthly client reports (`YYYY-MM.md`) and the AI prompt-check log.

**Nothing here yet.** The first report can't exist until Google Search Console has been collecting for a month — which can't start until §2 of `findability.md` is done.

**The AI prompt-check baseline should be recorded BEFORE any of the Phase 3 work**, so the improvement is provable. See `findability.md` §5.

Written by `/client-retainer`.
